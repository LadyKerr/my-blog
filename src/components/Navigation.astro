---
---
<nav>
    <div class="logo">
        DevBlog
        <div class="logo-dots">
            <span class="dot"></span>
            <span class="dot"></span>
            <span class="dot"></span>
            <span class="dot"></span>
        </div>
    </div>
    <div class="nav-links">
        <a href="/" class="active">Home</a>
        <a href="/about">About</a>
        <a href="/newsletter">Newsletter</a>
        <a href="/contact">Contact</a>
    </div>
</nav>

<style>
    nav {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1.5rem 2rem;
    }

    .logo {
        font-size: 1.8rem;
        font-weight: 700;
        color: #333;
        position: relative;
    }

    .logo-dots {
        display: flex;
        gap: 4px;
        position: absolute;
        top: 0;
        right: -24px;
    }

    .dot {
        width: 4px;
        height: 4px;
        border-radius: 50%;
    }

    .dot:nth-child(1) { background-color: #84fab0; }
    .dot:nth-child(2) { background-color: #8fd3f4; }
    .dot:nth-child(3) { background-color: #a8e6cf; }
    .dot:nth-child(4) { background-color: #dcedc1; }

    .nav-links {
        display: flex;
        gap: 2.5rem;
        align-items: center;
    }

    .nav-links a {
        text-decoration: none;
        color: #666;
        font-weight: 500;
        font-size: 1.1rem;
        transition: color 0.3s;
        position: relative;
    }

    .nav-links a:hover, .nav-links a.active {
        color: #333;
    }

    .nav-links a.active::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 100%;
        height: 2px;
        background: var(--primary-gradient);
    }

    @media (max-width: 768px) {
        nav {
            padding: 1rem;
        }

        .nav-links {
            display: none;
        }
    }
</style>